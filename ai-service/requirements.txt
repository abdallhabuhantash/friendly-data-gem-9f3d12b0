# Local AI service (Windows Server / Python 3.11+)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
httpx==0.28.1
# Supabase client set. Pinned as one mutually compatible bundle:
#   * >=2.16.0 is required for the opaque API-key formats (sb_publishable_* /
#     sb_secret_*); earlier releases reject them in create_client() because the
#     key had to be JWT-shaped.
#   * The sub-clients are pinned exactly because supabase >=2.16 passes
#     http_client= to postgrest/storage3/functions. A stale transitive
#     postgrest (e.g. 0.19.3 left over from supabase 2.11.0) raises
#     "SyncPostgrestClient.__init__() got an unexpected keyword argument
#     'http_client'" on the first table access. Explicit pins force pip to
#     upgrade those leftovers instead of keeping them.
# Do not loosen or downgrade these lines individually.
supabase==2.31.0
postgrest==2.31.0
storage3==2.31.0
realtime==2.31.0
supabase-auth==2.31.0
supabase-functions==2.31.0

opencv-python==4.10.0.84
numpy==1.26.4
ultralytics==8.3.55
# Install a CUDA build of torch separately for GPU inference, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
# Test/lint tooling lives in requirements-dev.txt, not here.
